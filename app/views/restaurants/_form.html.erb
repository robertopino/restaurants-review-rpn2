<%= form_with(model: restaurant) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
        <% restaurant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-2">
    <%= form.label :name %>
    <%= form.text_field :name, class:"form-control mb-2", placeholder:"Name" %>
  </div>

  <div class="form-group mb-2">
    <%= form.label :address %>
    <%= form.text_field :address, class:"form-control mb-2", placeholder:"Address" %>
  </div>

  <div class="form-group mb-2">
    <%= form.label :rating %>
    <%= form.number_field :rating, class:"form-control mb-2", placeholder:"Rating" %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
